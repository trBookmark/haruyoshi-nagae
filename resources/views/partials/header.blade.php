{{-- ヘッダー：筆記体のサイトタイトル（h1 は各ページのコンテンツ側で定義） --}}
<header class="l-header">
  <div class="l-header__site-name">
    <a class="l-header__link" href="{{ route('home') }}">{{ config('app.name') }}</a>
  </div>
</header>
